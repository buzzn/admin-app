import styled from 'styled-components';

export const TariffListWrapper = styled.div`
  margin-bottom: 4rem;
`;

export const AddTariffIcon = styled.i`
  cursor: pointer;
  margin-top: 0.45rem;
`;

export const ItemDetailsWrapper = styled.div`
  border: 1px solid #e5e5e5;
  padding: 5px;
  margin-top: 5px;
  .tariff-header {
    font-weight: bold;
    font-size: 18px;
    margin-top: 5px;
  }
`;
