import styled from 'styled-components';
import { Row } from 'reactstrap';

export const MarketFunctionType = styled.span`
  font-weight: bold;
  font-size: 1.4rem;
`;

export const MarketFunctionWrap = styled(Row)`
  margin-top: 0.5rem;
  border-top: 1px #e5e5e5 solid;
`;
