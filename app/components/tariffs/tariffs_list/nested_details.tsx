import React from 'react';
import { connect } from 'react-redux';
// @ts-ignore
import Contracts from 'contracts';

const NestedDetails = () => <div />;
// @ts-ignore
function mapStateToProps(state) {
  return {};
}

export default connect(mapStateToProps, {})(NestedDetails);
