import styled from 'styled-components';

export const EditOverlay = styled.div`
  opacity: 0.8;
  background: black;
  position: fixed;
  left: 0;
  top: 0;
  right: 0;
  bottom: 0;
  z-index: 2000;
  cursor: not-allowed;
`;
